# Configuration for Typescript bindings
#
# See https://graphql-code-generator.com/docs/getting-started/codegen-config

schema: ../../*.graphql
overwrite: true
generates:
  ./lib/types.ts:
    plugins:
      - typescript-common
      - typescript-client
